A simple practice project using GitActions to automate a docker containerization

To run it, pull the image:
```
docker pull aoiren/helloworld:latest
docker run -p 8080:8080 aoiren/helloworld:latest
```
